import React from 'react';

//sfc 컴포넌트 함수 선언 imr import 구문 자동 생성

const MyA = ({ a, b }) => {
  return (
    <div>
      <h1>MyA {`a = ${a}`}</h1>
      <h1>MyA {`b = ${b}`}</h1>
    </div>
  );
};

export default MyA;
